#!/bin/bash
# mind — Calibos' private cognitive tool.
# Runs from its own directory so the package imports regardless of cwd.
cd "$(dirname "$0")" || exit 1
exec ./.venv/bin/python -m calibos_mind.cli "$@"
